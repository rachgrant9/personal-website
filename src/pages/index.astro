---
import fs from 'node:fs';
import path from 'node:path';

// Read config file
const configPath = path.join(process.cwd(), 'config.txt');
const configContent = fs.readFileSync(configPath, 'utf-8');
const config: Record<string, string> = {};
configContent.split('\n').forEach(line => {
  if (line.trim() && !line.startsWith('#')) {
    const [key, ...valueParts] = line.split('=');
    if (key && valueParts.length) {
      config[key.trim()] = valueParts.join('=').trim();
    }
  }
});

// Read CV file
const cvPath = path.join(process.cwd(), 'cv.txt');
const cvContent = fs.readFileSync(cvPath, 'utf-8');

// Parse CV sections
interface ExperienceItem {
  title: string;
  company: string;
  period: string;
  description: string[];
}

const parseExperience = (cvText: string): ExperienceItem[] => {
  const experiences: ExperienceItem[] = [];
  const experienceSection = cvText.split('## EXPERIENCE')[1]?.split('##')[0] || '';
  const jobs = experienceSection.split('###').filter(j => j.trim());
  
  jobs.forEach(job => {
    const lines = job.split('\n').filter(l => l.trim());
    if (lines.length > 0) {
      const titleLine = lines[0].split('|').map(s => s.trim());
      const periodLine = lines[1]?.replace(/\*/g, '').trim() || '';
      const description = lines.slice(2).filter(l => l.startsWith('-')).map(l => l.substring(1).trim());
      
      experiences.push({
        title: titleLine[0] || '',
        company: titleLine[1] || '',
        period: periodLine,
        description
      });
    }
  });
  
  return experiences;
};

const experiences = parseExperience(cvContent);

// Extract name from CV
const nameMatch = cvContent.match(/# (.+?) - Curriculum Vitae/);
const fullName = nameMatch ? nameMatch[1] : 'Rachel Grant';

// Extract summary
const summaryMatch = cvContent.split('## PROFESSIONAL SUMMARY')[1]?.split('##')[0];
const summary = summaryMatch?.trim() || '';
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content={config.SITE_DESCRIPTION || 'Portfolio Website'}>
  <title>{config.SITE_TITLE || 'Portfolio'}</title>
</head>
<body class="bg-gray-50 text-gray-900">
  <!-- Header/Navigation -->
  <nav class="fixed w-full bg-white/90 backdrop-blur-sm shadow-sm z-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="#" class="text-xl font-bold text-blue-600">{fullName}</a>
        <div class="hidden md:flex space-x-8">
          <a href="#introduction" class="text-gray-700 hover:text-blue-600 transition">Introduction</a>
          <a href="#experience" class="text-gray-700 hover:text-blue-600 transition">Experience</a>
          <a href="#links" class="text-gray-700 hover:text-blue-600 transition">Links</a>
          <a href="#contact" class="text-gray-700 hover:text-blue-600 transition">Contact</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Introduction Section -->
  <section id="introduction" class="pt-32 pb-20 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6">
        Hello, I'm <span class="text-blue-600">{fullName}</span>
      </h1>
      <p class="text-xl md:text-2xl text-gray-600 mb-8 leading-relaxed">
        {summary}
      </p>
      <div class="flex justify-center gap-4">
        <a href="#contact" class="bg-blue-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-blue-700 transition">
          Get in Touch
        </a>
        <a href="#experience" class="border-2 border-blue-600 text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition">
          View Experience
        </a>
      </div>
    </div>
  </section>

  <!-- Experience Section -->
  <section id="experience" class="py-20 bg-white px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-4xl font-bold text-gray-900 mb-12 text-center">Experience</h2>
      <div class="space-y-12">
        {experiences.map((exp) => (
          <div class="border-l-4 border-blue-600 pl-6 py-2">
            <div class="flex flex-col md:flex-row md:justify-between md:items-start mb-3">
              <div>
                <h3 class="text-2xl font-bold text-gray-900">{exp.title}</h3>
                <p class="text-lg text-blue-600 font-semibold">{exp.company}</p>
              </div>
              <p class="text-gray-600 italic mt-2 md:mt-0">{exp.period}</p>
            </div>
            <ul class="list-disc list-inside space-y-2 text-gray-700">
              {exp.description.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Links Section -->
  <section id="links" class="py-20 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-4xl font-bold text-gray-900 mb-12 text-center">Connect With Me</h2>
      <div class="grid md:grid-cols-2 gap-6">
        <a 
          href={config.GITHUB_URL} 
          target="_blank" 
          rel="noopener noreferrer"
          class="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition border-2 border-transparent hover:border-blue-600 group"
        >
          <div class="flex items-center space-x-4">
            <svg class="w-12 h-12 text-gray-900 group-hover:text-blue-600 transition" fill="currentColor" viewBox="0 0 24 24">
              <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <h3 class="text-xl font-bold text-gray-900 group-hover:text-blue-600 transition">GitHub</h3>
              <p class="text-gray-600">View my projects and contributions</p>
            </div>
          </div>
        </a>
        
        <a 
          href={config.LINKEDIN_URL} 
          target="_blank" 
          rel="noopener noreferrer"
          class="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition border-2 border-transparent hover:border-blue-600 group"
        >
          <div class="flex items-center space-x-4">
            <svg class="w-12 h-12 text-blue-700 group-hover:text-blue-600 transition" fill="currentColor" viewBox="0 0 24 24">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"></path>
            </svg>
            <div>
              <h3 class="text-xl font-bold text-gray-900 group-hover:text-blue-600 transition">LinkedIn</h3>
              <p class="text-gray-600">Connect with me professionally</p>
            </div>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="py-20 bg-white px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-4xl font-bold text-gray-900 mb-12 text-center">Get in Touch</h2>
      <div class="bg-gradient-to-r from-blue-600 to-blue-700 rounded-lg p-8 md:p-12 text-white text-center">
        <p class="text-xl mb-6">
          I'm always open to new opportunities and interesting projects.
        </p>
        <div class="space-y-4">
          <div>
            <p class="text-blue-100 mb-2">Email</p>
            <a href={`mailto:${config.EMAIL}`} class="text-2xl font-semibold hover:underline">
              {config.EMAIL}
            </a>
          </div>
          <div>
            <p class="text-blue-100 mb-2">Location</p>
            <p class="text-xl font-semibold">{config.LOCATION}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-gray-400 py-8 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
      <p>&copy; {new Date().getFullYear()} {fullName}. All rights reserved.</p>
      <p class="mt-2 text-sm">Built with Astro and Tailwind CSS</p>
    </div>
  </footer>
</body>
</html>
